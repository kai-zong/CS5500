import { View, Text } from 'react-native'
import React from 'react'

const Headers = (appName) => {
  return (
    <View>
      <Text>Welcomes to {appName}</Text>
    </View>
  )
}

export default Headers
